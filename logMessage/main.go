package logMessage

const (
	ServerReady         string = "Server started, listening on :80"
	RequestReceived     string = "Received HTTP request to /"
	FileRequestReceived string = "File request received: "
)
